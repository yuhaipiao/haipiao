apply plugin: 'org.springframework.boot'

dependencies {
    compile project(":common")
    compile project(":persist")
}

bootJar {
    mainClassName = 'com.haipiao.registartion.application.Application'
}