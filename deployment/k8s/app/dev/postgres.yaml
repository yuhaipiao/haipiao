apiVersion: v1
kind: Service
metadata:
  name: postgres-np
  namespace: testk8s
  labels:
    app: postgres
spec:
  type: NodePort
  ports:
    - port: 48375
      targetPort: 5432
  selector:
    statefulset.kubernetes.io/pod-name: haipiao-postgres-0
    app: postgres
