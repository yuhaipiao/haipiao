apply plugin: 'application'

sourceSets {
    test {
        java.srcDirs = ['src/test/java/unit']
    }
}

dependencies {
    compile project(':haipiao-entities')
}

// Define the main class for the application
mainClassName = 'com.haipiao.application.App'

